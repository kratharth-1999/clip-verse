import React from "react";
import LogoAndMenu from "./LogoAndMenu";

const Header = () => {
    return (
        <div className="px-4">
            <LogoAndMenu />
        </div>
    );
};

export default Header;
